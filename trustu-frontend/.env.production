# Production — direct API calls to the hosted backend.
# The backend must have CORS configured to allow the production frontend domain.
VITE_API_BASE_URL=https://rosybrown-goat-692345.hostingersite.com
VITE_APP_NAME=TrustU
VITE_APP_ENV=production
